# Playwright e2e artifacts — generated by `npm run test:e2e`. The runner
# itself is `@playwright/test`, kept committed; the outputs below are not.
playwright-report/
test-results/
playwright/.cache/
