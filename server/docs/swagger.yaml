basePath: /
host: localhost:8080
info:
  contact: {}
  description: A unified gateway for multiple Large Language Models with billing,
    memory, and proxy pool support.
  title: LLM Router Platform API
  version: "1.0"
paths: {}
securityDefinitions:
  BearerAuth:
    description: Type "Bearer" followed by a space and JWT token.
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
